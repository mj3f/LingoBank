import { LanguagePhrasePipe } from './language-phrase.pipe';

describe('LanguagePhrasePipe', () => {
	it('create an instance', () => {
		const pipe = new LanguagePhrasePipe();
		expect(pipe).toBeTruthy();
	});
});
